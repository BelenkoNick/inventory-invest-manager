spring:
  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: diana
    driverClassName: org.postgresql.Driver
    hikari:
      schema: cs_invest_dev